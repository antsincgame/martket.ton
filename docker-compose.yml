# Стек web + api для Coolify или локального docker compose (из корня репозитория).
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_APPWRITE_ENDPOINT: ${VITE_APPWRITE_ENDPOINT}
        VITE_APPWRITE_PROJECT_ID: ${VITE_APPWRITE_PROJECT_ID}
        VITE_COMMERCE_API_URL: ${VITE_COMMERCE_API_URL}
        VITE_TONCONNECT_MANIFEST_URL: ${VITE_TONCONNECT_MANIFEST_URL}
        VITE_TONFORGE_API_URL: ${VITE_TONFORGE_API_URL}
    ports:
      - "${WEB_HOST_PORT:-80}:80"
    restart: unless-stopped

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: ${API_PORT:-8081}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN}
      APPWRITE_ENDPOINT: ${APPWRITE_ENDPOINT}
      APPWRITE_PROJECT_ID: ${APPWRITE_PROJECT_ID}
      APPWRITE_API_KEY: ${APPWRITE_API_KEY}
      TREASURY_WALLET_ADDRESS: ${TREASURY_WALLET_ADDRESS}
      TONAPI_KEY: ${TONAPI_KEY}
      TONAPI_BASE_URL: ${TONAPI_BASE_URL:-https://tonapi.io/v2}
      PLATFORM_FEE_BPS: ${PLATFORM_FEE_BPS:-250}
      COMMERCE_ADMIN_SECRET: ${COMMERCE_ADMIN_SECRET}
    ports:
      - "${API_HOST_PORT:-8081}:${API_PORT:-8081}"
    restart: unless-stopped
